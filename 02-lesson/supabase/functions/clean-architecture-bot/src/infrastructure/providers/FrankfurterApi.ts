export class FrankfurterApi {  private baseUrl: string;  constructor(baseUrl = 'https://api.frankfurter.dev/v1/latest') {    this.baseUrl = baseUrl;  }  async getLatestRates(targetCurrency: string) {    const response = await fetch(`${this.baseUrl}?base=USD`);    if (!response.ok) {      throw new Error(`Failed to fetch rates: ${response.statusText}`);    }    return await response.json();  }}export const exchangeRateProvider = new FrankfurterApi();