export class ConversionResult {  baseCurrency: string;  targetCurrency: string;  amount: number;  result: number;  rate: number;  constructor(baseCurrency: string, targetCurrency: string, amount: number, result: number, rate: number) {    this.baseCurrency = baseCurrency.toUpperCase();    this.targetCurrency = targetCurrency.toUpperCase();    this.amount = amount;    this.result = result;    this.rate = rate;  }  isSameCurrency(): boolean {    return this.baseCurrency === this.targetCurrency;  }}