// Точка сборки (Composition Root)// Здесь мы связываем все модули согласно правилам Инверсии Контроля из Чистой Архитектуры.const env = require('./src/config/env');const FrankfurterApi = require('./src/infrastructure/providers/FrankfurterApi');const TelegramSender = require('./src/infrastructure/bot/TelegramSender');const HandleMessageUseCase = require('./src/application/useCases/HandleMessageUseCase');const WebhookController = require('./src/presentation/controllers/WebhookController');const FastifyApp = require('./src/infrastructure/server/FastifyApp');async function bootstrap() {  // 1. Инициализируем инфраструктурные сервисы (провайдеры)  const exchangeProvider = new FrankfurterApi();  const telegramSender = new TelegramSender(env.TELEGRAM_BOT_TOKEN);  // 2. Инициализируем бизнес-логику (Use Cases), внедряя (injecting) зависимости через конструктор  const handleMessageUseCase = new HandleMessageUseCase(exchangeProvider, telegramSender);  // 3. Инициализируем контроллеры, отдавая им нужные юзкейсы  const webhookController = new WebhookController(handleMessageUseCase);  // 4. Запускаем сервер, передавая ему контроллер  const server = new FastifyApp(webhookController);  await server.start(env.PORT);}bootstrap();