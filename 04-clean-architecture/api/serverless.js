const fastifyApp = require('../src/infrastructure/server/FastifyApp');// Экспортируем функцию-обработчик для Vercel Serverless Functionsmodule.exports = async (req, res) => {  // Инициализируем Fastify, если он еще не готов  await fastifyApp.app.ready();    // В Vercel (при использовании async функции) очень важно не возвращать Promise   // до тех пор, пока ответ (res) полностью не отправлен. Иначе Vercel может убить   // контейнер до того, как Fastify отправит ответ в Telegram.  return new Promise((resolve) => {    res.on('finish', resolve);    res.on('error', resolve);        // Прокидываем запрос в Fastify    fastifyApp.app.server.emit('request', req, res);  });};// Отключаем встроенный парсер Vercelmodule.exports.config = {  api: {    bodyParser: false,  },};