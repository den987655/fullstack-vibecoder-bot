const fastifyApp = require('../src/infrastructure/server/FastifyApp');// Экспортируем функцию-обработчик для Vercel Serverless Functionsmodule.exports = async (req, res) => {  // Дожидаемся инициализации всех плагинов и роутов Fastify  await fastifyApp.app.ready();    // Прокидываем стандартный (Node.js) HTTP request/response внутрь Fastify  fastifyApp.app.server.emit('request', req, res);};// Отключаем встроенный парсер тела запроса от Vercel. // Fastify сам должен прочитать поток данных (raw stream), // если Vercel распарсит его первым, Fastify просто зависнет в ожидании данных.module.exports.config = {  api: {    bodyParser: false,  },};