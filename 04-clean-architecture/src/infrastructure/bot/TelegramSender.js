const env = require('../../config/env');// Роутер/Адаптер для отправки сообщений (Infrastructure)class TelegramSender {  constructor(token) {    this.token = token || env.TELEGRAM_BOT_TOKEN;    this.apiUrl = `https://api.telegram.org/bot${this.token}/sendMessage`;  }  // Соответствует контракту IMessageSender  async sendMessage(chatId, text) {    if (!this.token) {      console.warn('Внимание: TELEGRAM_BOT_TOKEN не задан!');    }        const res = await fetch(this.apiUrl, {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({        chat_id: chatId,        text: text,        parse_mode: 'HTML'      })    });        if (!res.ok) {      console.error('Ошибка отправки в Telegram:', res.statusText);    }        return res;  }}module.exports = TelegramSender;