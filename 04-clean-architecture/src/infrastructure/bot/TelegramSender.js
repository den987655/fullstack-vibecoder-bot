const env = require('../../config/env');class TelegramSender {  constructor() {    this.token = env.TELEGRAM_BOT_TOKEN;    this.apiUrl = `https://api.telegram.org/bot${this.token}`;  }  async sendMessage(chatId, text, options = {}) {    if (!this.token) console.warn('Внимание: TELEGRAM_BOT_TOKEN не задан!');        return await fetch(`${this.apiUrl}/sendMessage`, {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({        chat_id: chatId,        text: text,        parse_mode: 'HTML',        ...options // Для передачи reply_markup (inline-кнопок)      })    });  }  async editMessageText(chatId, messageId, text, options = {}) {    return await fetch(`${this.apiUrl}/editMessageText`, {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({        chat_id: chatId,        message_id: messageId,        text: text,        parse_mode: 'HTML',        ...options      })    });  }  async answerCallbackQuery(callbackQueryId, text = '', showAlert = false) {    return await fetch(`${this.apiUrl}/answerCallbackQuery`, {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({        callback_query_id: callbackQueryId,        text: text,        show_alert: showAlert      })    });  }}module.exports = new TelegramSender();