const webhookController = require('../../presentation/controllers/WebhookController');// We are now only registering routes on the provided Fastify instance // so that Vercel can directly "see" and introspect the application via server.js.module.exports = function setupRoutes(app) {  app.get('/', async (request, reply) => {    return reply.send({ status: 'Platform is running', bot: 'Currency Rate Bot' });  });  app.post('/webhook/telegram', async (request, reply) => {    return webhookController.handle(request, reply);  });};