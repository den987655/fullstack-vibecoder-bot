const Fastify = require('fastify');const env = require('../../config/env');const webhookController = require('../../presentation/controllers/WebhookController');class FastifyApp {  constructor() {    this.app = Fastify({ logger: false });    this.setupRoutes();  }  setupRoutes() {    // В Fastify v4/v5 правильный синтаксис catch-all это '/*'     this.app.all('/*', async (request, reply) => {            // Все POST-запросы отправляем в WebhookController       if (request.method === 'POST') {        return webhookController.handle(request, reply);      }            // Для GET-запросов отдаем health-check      return reply.send({         status: 'Platform is running',         bot: 'Currency Rate Bot',        vercel_url_received: request.url       });    });  }  async start() {    try {      await this.app.listen({ port: env.PORT, host: '0.0.0.0' });      console.log(`Сервер без Dependency Injection запущен на порту ${env.PORT}`);    } catch (err) {      console.error(err);      process.exit(1);    }  }}module.exports = new FastifyApp();