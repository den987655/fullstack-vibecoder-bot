// Провайдер, реализующий логику получения курсов валют (Infrastructure)class FrankfurterApi {  constructor(baseUrl = 'https://api.frankfurter.dev/v1/latest') {    this.baseUrl = baseUrl;  }  async getLatestRates(targetCurrency) {    const response = await fetch(`${this.baseUrl}?base=USD`);    if (!response.ok) {      throw new Error(`Failed to fetch rates: ${response.statusText}`);    }    return await response.json();  }}// Экспортируем готовый объект (Singleton)module.exports = new FrankfurterApi();