class ConversionResult {  constructor(baseCurrency, targetCurrency, amount, result, rate) {    this.baseCurrency = baseCurrency.toUpperCase();    this.targetCurrency = targetCurrency.toUpperCase();    this.amount = amount;    this.result = result;    this.rate = rate;  }  isSameCurrency() {    return this.baseCurrency === this.targetCurrency;  }}module.exports = ConversionResult;