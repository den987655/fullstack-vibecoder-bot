# C4 Model: Components Diagram```plantuml@startuml C4_Components!include https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/master/C4_Component.puml' ============================================================' ВИЗУАЛЬНЫЙ СТИЛЬ И ПАЛИТРА (На основе референса)' ============================================================skinparam backgroundColor #FFFFFFskinparam shadowing falseskinparam linetype orthoskinparam nodesep 70skinparam ranksep 70skinparam padding 15skinparam TitleFontSize 20' Отключаем громоздкие стереотипы (<<component>>) для чистотыHIDE_STEREOTYPE()' Настройка стрелок связей (тонкие, аккуратные)skinparam arrow {    Color #666666    Thickness 1    FontSize 13    FontColor #333333}' Принудительно задаем цвета: Синие для компонентов, Серые для внешних системUpdateElementStyle("component", $bgColor="#1168BD", $fontColor="#FFFFFF", $borderColor="#08498A")UpdateElementStyle("external_system", $bgColor="#F2F2F2", $fontColor="#222222", $borderColor="#999999")' ============================================================' УЗЛЫ И КОМПОНЕНТЫ' ============================================================System_Ext(telegram_in, "Telegram API", "External System")System_Boundary(bot, "Currency Rate Bot") {        Component(app, "Fastify App", "Node.js", "Server Instance")    Component(webhook, "Webhook Route", "Handler", "Endpoints")        Component(usecase, "Handle Message", "Use Case", "Business Logic")        together {        Component(tg_sender, "Telegram Sender", "Adapter", "API Client")        Component(rate_provider, "Rate Provider", "Adapter", "API Client")    }}System_Ext(telegram_out, "Telegram API", "External System")System_Ext(ff, "Frankfurter API", "External System")' ============================================================' МАРШРУТИЗАЦИЯ СВЯЗЕЙ (LAYOUT)' ============================================================' Строгое размещение Telegram слева от Webhooktelegram_in -[hidden]R-> webhook' Связи (точно как в ASCII чертеже)Rel_R(telegram_in, webhook, "Webhook POST")Rel_D(app, webhook, "Mounts")Rel_D(webhook, usecase, "Handle message")Rel_D(usecase, tg_sender, "Send reply")Rel_D(usecase, rate_provider, "Get exchange rate")Rel_D(tg_sender, telegram_out, "API Call")Rel_D(rate_provider, ff, "API Call")@enduml```