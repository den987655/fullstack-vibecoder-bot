# C4 Model: Components Diagram (Level 3)```mermaidC4Component  title Component Diagram — Currency Bot  Container_Ext(telegram, "Telegram API", "Платформа")  Container_Ext(ff, "Frankfurter API", "Провайдер")  Container_Boundary(bot_server, "Bot Server — Node.js / Fastify") {        System_Boundary(presentation, "Presentation — HTTP delivery") {      Component(app, "Fastify App", "JS Module", "Создает инстанс и роуты")      Component(webhook, "Telegram Webhook Route", "Fastify Handler", "POST /webhook/telegram")    }        System_Boundary(application, "Application — Core") {      Component(usecase, "Handle Message", "Use Case", "Парсинг и математика")    }        System_Boundary(infrastructure, "Infrastructure — Adapters") {      Component(tg_sender, "Telegram Sender", "Adapter", "Адаптер для Telegram")      Component(api_provider, "Rate Provider", "Adapter", "HTTP-шлюз Frankfurter")    }  }  Rel_D(app, webhook, "Mounts")  Rel_R(telegram, webhook, "POST payload")  Rel_D(webhook, usecase, "Parses & Invokes")  Rel_D(usecase, api_provider, "getRates()")  Rel_D(usecase, tg_sender, "sendMessage()")    Rel_D(api_provider, ff, "GET /latest")  Rel_R(tg_sender, telegram, "POST requests")```