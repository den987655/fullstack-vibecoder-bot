# C4 Model: Context Diagram (Level 1)```mermaidC4Context  title Context Diagram — Currency Bot  Person(user, "User", "Пользователь бота")  System_Ext(telegram, "Telegram", "Мессенджер")    System(bot, "Currency Bot", "Конвертирует валюты")  System_Ext(ff, "Frankfurter", "Данные ЕЦБ")  Rel_D(user, telegram, "Сообщения", "Mobile/Web")  Rel_D(telegram, bot, "Передает Webhook", "HTTPS")  Rel_U(bot, telegram, "Шлет ответы", "HTTPS")  Rel_R(bot, ff, "Запрашивает курсы", "HTTPS")```