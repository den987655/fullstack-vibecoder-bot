const http = require('http');const PORT = 3000;const server = http.createServer((req, res) => {    // Устанавливаем заголовки, чтобы клиент понимал, что в ответ приходит JSON    res.setHeader('Content-Type', 'application/json; charset=utf-8');    // Получаем текущее время    const currentTime = new Date().toLocaleString('ru-RU');    // Обрабатываем роуты через switch-case    switch (req.url) {        case '/hello':            res.writeHead(200);            // Возвращаем JSON только с hello message            res.end(JSON.stringify({                 message: "Hello message"             }));            break;                    case '/time':            res.writeHead(200);            // Возвращаем JSON только с текущим временем            res.end(JSON.stringify({                 time: currentTime             }));            break;                    case '/info':            res.writeHead(200);            // Возвращаем JSON и с hello message, и с текущим временем            res.end(JSON.stringify({                 message: "Hello message",                 time: currentTime             }));            break;                    default:            res.writeHead(404);            res.end(JSON.stringify({                 error: "Эндпоинт не найден (404)"             }));            break;    }});server.listen(PORT, () => {    console.log(`Сервер запущен на http://localhost:${PORT}`);    console.log('Доступные эндпоинты:');    console.log('- http://localhost:3000/hello');    console.log('- http://localhost:3000/time');    console.log('- http://localhost:3000/info');});