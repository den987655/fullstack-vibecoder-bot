const TOKEN = process.env.TELEGRAM_BOT_TOKEN || 'YOUR_BOT_TOKEN_HERE';let offset = 0;async function getUpdates() {  try {    const response = await fetch(`https://api.telegram.org/bot${TOKEN}/getUpdates?offset=${offset}&timeout=30`);        if (!response.ok) {      throw new Error(`HTTP error! status: ${response.status}`);    }        const data = await response.json();        if (data.ok && data.result.length > 0) {      for (const update of data.result) {        console.log('Новый апдейт:', JSON.stringify(update, null, 2));        // Update the offset to acknowledge the message        offset = update.update_id + 1;      }    }  } catch (error) {    console.error('Ошибка при получении апдейтов:', error.message);  } finally {    // В случае ошибки ждем секунду и пробуем снова. При успехе идем на следующий пул.    setTimeout(getUpdates, 1000);  }}console.log('Бот запущен. Ожидание апдейтов...');getUpdates();